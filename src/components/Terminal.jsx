import { Terminal as TerminalIcon } from 'lucide-react'
import { motion, useReducedMotion } from 'framer-motion'

export default function Terminal({ security = false }) {
  const reduceMotion = useReducedMotion()
  const lines = security ? [['security --focus', 'Networking'], ['environment', 'Controlled Laboratory'], ['objective', 'Learn · Build · Understand']] : [['whoami', 'Muhammad Syafiq'], ['role', 'Hospitality / Web Development / Cybersecurity'], ['location', 'Karawang, Indonesia'], ['status', 'Open to Opportunities']]
  return <motion.div className="terminal" aria-label="Profile terminal" initial={reduceMotion ? false : { opacity: 0, y: 18 }} animate={reduceMotion ? undefined : { opacity: 1, y: 0 }} transition={{ duration: .6, ease: [0.22, 1, 0.36, 1], delay: .18 }}><div className="terminal-top"><span className="terminal-dots"><i /><i /><i /></span><span><TerminalIcon size={14} /> {security ? 'learning-lab' : 'syafiq-profile'}</span></div><div className="terminal-body">{lines.map(([command, result], index) => <motion.div className="terminal-line" key={command} initial={reduceMotion ? false : { opacity: 0, x: -8 }} animate={reduceMotion ? undefined : { opacity: 1, x: 0 }} transition={{ duration: .35, delay: .42 + index * .12 }}><span className="terminal-command">$ {command}</span>{security && command === 'security --focus' ? <div className="terminal-result">&gt; {result}<br />&gt; Linux<br />&gt; Web Security<br />&gt; Cybersecurity Fundamentals</div> : <span className={result === 'Open to Opportunities' ? 'terminal-result accent-text' : 'terminal-result'}>{result}</span>}</motion.div>)}</div></motion.div>
}